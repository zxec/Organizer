https://validator.w3.org/nu/#textarea
выдаёт ошибки по index.html, надо править

есть файлы в репозитории которых быть не должно, например .sass-cache папка лишняя
style.css, style.css.map лишние, эти файлы должны генерится мною например через npm run build локально, а у тебя должны быть в .gitignore

в целом не очень качественно проработана структура лейаута, при таком подходе довольно сложно модифицировать проект,
например ты можешь убедиться если уменьшишь высоту экрана, то контент сожмётся без всяких отступов
нет адаптива для мобилок, появляется горизонтальный скролл, надо использовать подход mobile first
если появится шапка или футер, то выбранный подход в принципе не заработает, т.к. не рассчитан на модификацию структуры, её придётся менять

в целом сильно заметно отсутствие понимания БЭМ, очень много модификаторов которые либо должны быть элементами, либо должны быть блоками,
модификатор изменяет дефолтное поведение уже написаного кода и модификатор можно удалить из кода, а у тебя зачастую нет

на макете не представлен вариант как выглядят результаты поиска,
предполагаю если кто-то использует поиск, то текущий открытый день ему не нужен,
но нужно выдать сгруппированные по дням таски
